// build a router only for adminstartion here, outside the mobile app. you must be able to block, delete, suspende, modify any user of thye app 

// import express from 'express';


// const router = express.Router();
// import * as factory from './handlerFactory';

// //something-not-easy-to-guess = admin; 
// router.use(coffeeAuthController.restrictTo('something-not-easy-to-guess'));


// router
//   .route('/')
//   .get(userController.getAllUsers)
//   .post(userController.createUser);

// router
//   .route('/:id')
//   .get(userController.getUser)
//   .patch(userController.updateUser)
//   .delete(userController.deleteUser);
  
  // move thsi to a separta file only for admin
  // one more for block user for n time
// export const getUser: any = factory.getOne(User);
// export const getAllUsers = factory.getAll(User);

// // Do NOT update passwords with this!
// export const updateUser = factory.updateOne(User);
// export const deleteUser = factory.deleteOne(User);